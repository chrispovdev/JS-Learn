const country = "Panama";
const continent = "America";
let population = 4000000;
console.log(
  `I live in ${country} and its continent is ${continent}, its total population is ${population}.`
);

let isIsland = false;
let language = "Spanish";
console.log(isIsland, population, country, language);

// const continent = "Europe"; //can't change const variable
